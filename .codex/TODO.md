# TODO

This file is the live status board for work in this repo.

Use these statuses:

- `🟦 Todo`: defined but not started
- `🟨 In Progress`: implementation is actively underway
- `🟪 QA Ready`: implementation is complete and waiting for QA confirmation
- `✅ Done`: fully tested and confirmed

## Workflow

1. Read `README.md`.
2. Read `.codex/canonical-plan/README.md`.
3. Read only the canonical plan files relevant to the task.
4. Compare the requested task against the current canonical plan before adding it to this tracker as a normal item.
5. If the task conflicts with the current canon:
   - stop
   - explain the mismatch clearly
   - ask whether to update the canonical plan or adjust the task
   - do not add it as a normal `TODO` item
   - mark it as blocked or pending canon decision
   - do not code until that decision is made
6. If the task is aligned:
   - add it as a normal `TODO` item
   - move it to `In Progress` before making changes
   - implement the smallest non-breaking change that satisfies the task
   - add or update tests when behavior changes
   - run the smallest meaningful verification
7. Move an item to `QA Ready` only when implementation is complete, local verification has passed, and a commit is prepared using the repo's commit style guide in `CONTRIBUTING.md`.
8. Move an item to `Done` only after the change has been properly tested and confirmed.

## Status Board

| ID       | Task                                                                                                                                                                                                                                                               | Status         | Latest note                                                                                                                                                                                                                                                                                       |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| STK-0001 | Establish root TODO workflow with canonical-plan preflight and status gates                                                                                                                                                                                        | ✅ Done        | `TODO.md` created; workflow captures canon check, mismatch stop rule, implementation flow, and verification gates.                                                                                                                                                                                |
| STK-0002 | Add optional collection index output for dynamic and catch-all routes, with selectable exposed keys                                                                                                                                                                | ✅ Done        | Local canon updated first; CLI now supports `config.listIndex`; verified with targeted build tests and syntax checks.                                                                                                                                                                             |
| STK-0003 | Require a repo-style commit before moving work to `QA Ready`                                                                                                                                                                                                       | ✅ Done        | `TODO.md` now requires a `CONTRIBUTING.md`-style commit at the `QA Ready` gate.                                                                                                                                                                                                                   |
| STK-0004 | Add global collection index controls via CLI flags and `statikapi.config.js`, with local route config overriding global defaults                                                                                                                                   | ✅ Done        | Implemented and verified locally; route config overrides global defaults from config and flags; prepared commit subject: `feat(config): add global list index defaults`.                                                                                                                          |
| STK-0005 | Expand the Cloudflare adapter and `create-statikapi` flow to cover serving-mode selection, `/public` partitioning, public/private routing, auth-header envs, webhook controls, deploy wiring, Cloudflare account/token onboarding, usage limits, and matching docs | ✅ Done        | Implemented in `create-statikapi`, `@statikapi/adapter-cf`, example Cloudflare app, and public docs; verified with scaffold tests, adapter runtime tests, and syntax checks; prepared commit: `feat(cloudflare): expand worker and scaffold contract`.                                            |
| STK-0006 | Add `config.listIndex` support to `@statikapi/adapter-cf`, including project defaults, route overrides, derived collection/index outputs, and manifest coverage                                                                                                    | 🟪 QA Ready    | Canon updated first; adapter runtime now emits collection indexes in Cloudflare builds and targeted rebuilds; verified with `node --test packages/adapter-cloudflare/test/runtime.test.js`; prepared commit subject: `feat(cloudflare): add collection index outputs`.                            |
| STK-0007 | Add Cloudflare local preview parity with the shared UI through a preview proxy, Worker manifest/route forwarding, local private-route auth injection, and refresh behavior                                                                                         | 🟪 QA Ready    | Canon updated first; `statikapi-cf preview` now serves `/_ui/`, scaffolded Cloudflare apps run it during `pnpm dev`, and preview behavior is verified with adapter preview/runtime tests plus Cloudflare scaffold coverage; prepared commit subject: `feat(cloudflare): add local preview proxy`. |
| STK-0008 | Simplify the Cloudflare contract to a single Worker + Static Assets model with public-by-default routes, no `r2-public` mode, and private routes behind the Worker                                                                                                 | 🟪 QA Ready    | Scaffold, runtime, tests, docs, and QA now target the single Static Assets model. Public routes are emitted at build time; private routes remain Worker-managed.                                                                                                                                  |
| STK-0009 | Make Cloudflare `dev` reliably launch the shared preview UI by default across package managers, open it like regular local dev, and keep previewed outputs updating as source changes rebuild them                                                                 | 🟪 QA Ready    | Canon updated first; adapter now has a first-class `dev` command and scaffold scripts point to it, but final end-to-end local confirmation is still pending.                                                                                                                                      |
| STK-0010 | Split Cloudflare manifests into public Static Assets at `/public/_manifest` and private Worker-managed data at `/_manifest`                                                                                                                                        | 🟪 QA Ready    | Public manifest should be a static asset that exposes only public routes; private manifest should move off `/manifest` and remain Worker-managed under `/_manifest`.                                                                                                                              |
| STK-0011 | Make Cloudflare preview use correct fetch URLs and JSON paths, including `STATIK_USE_INDEX_JSON` handling for public assets                                                                                                                                        | 🟪 QA Ready    | The preview UI must stop guessing `index.json` paths for Cloudflare routes and instead show/fetch the actual asset URL for public routes while keeping private Worker routes correct.                                                                                                             |
| STK-0012 | Change Cloudflare webhook routing so `POST /` rebuilds all routes and `POST <route-path>` rebuilds that route on the same path                                                                                                                                     | 🟪 QA Ready    | Canon updated first; adapter runtime, tests, QA, and scaffold docs now use route-path `POST` webhooks instead of `/build`; verified with adapter runtime/preview tests.                                                                                                                           |
| STK-0013 | Fix Cloudflare `dev` so route-content edits actually propagate to Worker output and preview data without requiring a reinstall or cold restart                                                                                                                     | 🟪 QA Ready    | `statikapi-cf dev` now restarts the local Wrangler runtime after successful source rebuilds so changed Worker/assets output can be picked up without reinstalling; verified with `node --check packages/adapter-cloudflare/bin/statikapi-cf.js`.                                               |
| STK-0014 | Group Cloudflare preview routes by public vs private visibility so users can clearly distinguish the two surfaces                                                                                                                                                  | 🟪 QA Ready    | The shared preview UI now renders separate Cloudflare route groups for public Static Assets vs private Worker routes while leaving the regular local CLI sidebar unchanged.                                                                                                                       |
| STK-0015 | Fix Cloudflare preview Absolute URL/snippet generation so it uses the Worker origin, respects route-shape preferences, and shows auth requirements for private routes                                                                                              | 🟪 QA Ready    | Cloudflare preview metadata now carries the private auth header name and the shared snippet generator uses the Worker origin plus explicit private-route auth headers; verified with `node --test packages/adapter-cloudflare/test/preview.test.js` and `node --check packages/adapter-cloudflare/src/node/preview.js`.                |
| STK-0016 | Expand Cloudflare docs to cover pricing/free limits, overage billing behavior, custom domains, and deployment instructions for scaffolded Worker projects                                                                                                          | 🟪 QA Ready    | Cloudflare docs now describe the Worker/Static Assets/KV/R2 pricing surfaces, free-tier expectations, overage behavior, custom-domain setup, scaffold deploy steps, and production checklists, with official Cloudflare references and QA coverage updated.                                        |
| STK-0017 | Review the entire docs section for clarity, missing details, confusing explanations, and layout quality, then tighten it into a more complete 1.0-level docs set                                                                                                  | 🟪 QA Ready    | Completed a broad docs audit and patched multiple issues: stale route/function contracts, misleading examples, branch-name drift in deployment/community docs, thin CLI overview guidance, and incomplete Cloudflare deployment/cost explanations. Verify with the docs QA pass before closing.                                   |
| STK-0018 | Fix `statikapi --help` output so it cleanly shows the canonical CLI surface, with a separate `Commands:` section listing only `build` and `dev`                                                                                                                 | 🟪 QA Ready    | Help text now renders a clean `Commands:` section with only `build` and `dev`; verified with `node --test packages/cli/test/cli.test.js` and `node packages/cli/bin/statikapi.js --help`; prepared commit subject: `fix(cli): clean up help output`.                                           |
| STK-0019 | Fix Cloudflare local preview so routes with `config.cloudflare.webhook = false` still appear in the UI and remain readable even though webhook rebuilds for those routes are disabled                                                                           | 🟪 QA Ready    | Added a loopback-only preview build path so local preview/dev can seed private outputs without broadening normal webhook behavior; verified with `node --test packages/adapter-cloudflare/test/preview.test.js` and `node --test packages/adapter-cloudflare/test/runtime.test.js`; prepared commit subject: `fix(cloudflare): include webhook-disabled routes in preview`. |
| STK-0020 | Align Cloudflare `STATIK_USE_INDEX_JSON = false` output paths with canon so non-root routes emit extensionless keys like `posts` instead of `posts.json`                                                                                                        | 🟪 QA Ready    | False mode now removes `.json` from route-facing public/private paths and manifest references; public Static Assets use collision-safe hidden `index` files under those extensionless route paths while private keys remain extensionless; verified with `node --test packages/adapter-cloudflare/test/preview.test.js` and `node --test packages/adapter-cloudflare/test/runtime.test.js`; prepared commit subject: `fix(cloudflare): align false-mode route paths`. |
| STK-0021 | Update Cloudflare docs to state the exact route/output path behavior for `STATIK_USE_INDEX_JSON`, including the extensionless `false` mode once runtime behavior is aligned                                                                                      | 🟪 QA Ready    | Updated the scaffold README and Cloudflare adapter guide to describe false-mode extensionless public/private paths, `/public/_manifest`, and the hidden `index` backing files used for public asset collisions; verified with a stale-example grep and manual diff review; prepared commit subject: `docs(cloudflare): clarify false-mode route paths`. |
| STK-0022 | Change Cloudflare scaffolding to generate a real `.dev.vars` file directly instead of `.dev.vars.example`, and document clearly how those local/dev and deploy-time variables should be set for deployed Workers                                               | 🟪 QA Ready    | The Cloudflare scaffold now writes `.dev.vars` directly, the scaffold/adapter docs explain local `.dev.vars` versus deploy-time Worker config, and `node --test packages/create-statikapi/test/scaffold-cloudflare.test.js` passes; prepared commit subject: `fix(create-statikapi): scaffold .dev.vars directly`.                |
| STK-0023 | Remove duplicate Cloudflare auth/build variable definitions from `wrangler.toml` and `.dev.vars`, keeping `STATIK_BUILD_TOKEN`, `STATIK_PRIVATE_AUTH_HEADER_NAME`, and `STATIK_PRIVATE_AUTH_HEADER_VALUE` in one canonical place                               | 🟪 QA Ready    | The scaffold now keeps those three auth/build values in `.dev.vars` only and removes them from scaffolded `wrangler.toml`; docs now explain that deployed Worker auth/build values must be set separately in Cloudflare; verified with `node --test packages/create-statikapi/test/scaffold-cloudflare.test.js`; prepared commit subject: `fix(create-statikapi): dedupe cloudflare auth vars`. |
| STK-0024 | Fix Cloudflare preview route details so Absolute URL plus `curl`, browser `fetch`, and Node `fetch` examples respect `STATIK_USE_INDEX_JSON = false` during local dev                                                                                          | 🟪 QA Ready    | The preview snippet URL builder now uses Cloudflare `uiMeta.useIndexJson` so Absolute URL plus curl/browser/node examples follow the active public/private route surface in both false and true modes; verified with `node --test packages/ui/test/snippets.test.js`; prepared commit subject: `fix(ui): respect cloudflare route shape in snippets`. |
| STK-0025 | Carefully investigate and fix the Cloudflare `dev` regression where local preview/build loops now fire repeated `POST /_preview/build` plus manifest fetches without any source change, causing route output to keep updating chaotically instead of idling cleanly like `1.0.0-rc.1` | 🟪 QA Ready    | Fixed the preview-side rebuild feedback loop by making private-output seeding idempotent per preview-server session instead of retriggering on every `/ui/index` manifest read; verified with `node --test packages/adapter-cloudflare/test/preview.test.js`; prepared commit subject: `fix(cloudflare): stop repeated preview rebuilds`. |
| STK-0026 | Carefully fix the Cloudflare deploy contract so public assets are built before deploy, deployed secrets are documented clearly, and private routes can be seeded after deploy without breaking anything else                                                   | 🟪 QA Ready    | The scaffolded `deploy` script now uses `statikapi-cf deploy` to rebuild public assets before `wrangler deploy`, load local deploy envs from `.dev.vars`, and optionally seed private outputs when `STATIK_DEPLOY_ORIGIN` is set; docs now explain deployed secret setup via Wrangler or the dashboard plus the manual/private-seed fallback; verified with `node --test packages/adapter-cloudflare/test/deploy.test.js packages/adapter-cloudflare/test/preview.test.js packages/create-statikapi/test/scaffold-cloudflare.test.js` and `node --check packages/adapter-cloudflare/bin/statikapi-cf.js`; prepared commit subject: `fix(cloudflare): wire safer deploy flow`. |
| STK-0027 | Make Cloudflare deploy treat private-output seeding as best-effort, so a missing deployed origin, missing build token, or transient 404 after `wrangler deploy` does not fail the overall deploy command; document the manual secret and post-deploy seeding steps clearly | 🟪 QA Ready | `statikapi-cf deploy` now warns instead of failing when the optional post-deploy seed cannot run, and the Cloudflare docs now call out the required deployed Worker secrets plus the manual `statikapi-cf rebuild --worker ...` fallback. |
| STK-0028 | Document Git-connected Cloudflare deployments for scaffolded Worker projects, including what belongs in Workers Builds vs deployed Worker secrets, and how `pnpm deploy` behaves when the repo is deployed from a connected Git provider | 🟪 QA Ready | Added the Git-connected deployment guidance to the Cloudflare docs and scaffold README; verified by targeted grep across the updated deployment sections. Prepared commit subject: `docs(cloudflare): document git-connected deploys`. |
| STK-0029 | Update `README.md` to explain usage more clearly, improve the page structure and visual positioning, and explore lightweight animation or motion cues that make the project feel faster to understand | 🟪 QA Ready | README now leads with a user-facing workflow, keeps repo/contributor details behind links, and points maintainers to [CONTRIBUTING.md](CONTRIBUTING.md) plus the docs/canonical-plan notes; verified with `git diff --check`; prepared commit subject: `docs(repo): split README audience`. |
| STK-0030 | Fix `create-statikapi --template cloudflare-adapter` so custom `--assets-dir` and `--src-dir` choices are respected end-to-end, including generated example file placement and any scaffolded scripts or config fields that still hardcode `public` or `src-api`; audit for similar ignored scaffold inputs | 🟪 QA Ready | Generated Cloudflare scaffolds now thread custom source/assets directories through `wrangler.toml`, `package.json`, `README.md`, and `.dev.vars`; verified with `node --test packages/create-statikapi/test/scaffold-cloudflare.test.js`; prepared commit subject: `fix(create-statikapi): respect cloudflare scaffold dirs`. |
