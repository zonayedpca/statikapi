# Example: Basic

A single static endpoint at `/`.

## Run

```
pnpm dev        # in one terminal (watches src-api and writes to api-out)
pnpm build      # to build

Or build once:

pnpm dev
pnpm build
```
