# Example: Dynamic

Demonstrates:

- `/` (static)
- `/users/:id` (dynamic via `paths()`)
- `/docs/*slug` (catch-all via `paths()`)

## Run

```
pnpm dev        # watches and rebuilds
pnpm build      # to build

Build once:
pnpm build
```
