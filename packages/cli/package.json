{
  "name": "statikapi",
  "version": "0.1.0",
  "type": "module",
  "bin": { "statikapi": "bin/statikapi.js" },
  "files": ["bin", "dist", "ui", "src", "README.md", "LICENSE"],
  "exports": { ".": { "import": "./src/index.js" } },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "repository": "github:<YOUR_ORG>/<YOUR_REPO>",
  "bugs": { "url": "https://github.com/<YOUR_ORG>/<YOUR_REPO>/issues" },
  "homepage": "https://github.com/<YOUR_ORG>/<YOUR_REPO>#readme",
  "keywords": ["static", "json", "api", "cli", "ssg"],
  "publishConfig": { "access": "public", "provenance": true },
  "dependencies": {
    "chokidar": "^3.6.0"
  },
  "scripts": {
    "build": "node ./scripts/build.js || true",
    "sync-ui": "rm -rf ui && mkdir -p ui && cp -R ../ui/dist/* ui/",
    "prepack": "pnpm -w -C packages/ui build && pnpm -C packages/cli sync-ui"
  }
}
